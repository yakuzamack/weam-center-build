// Stub i18n file (next-intl removed). Keep locale metadata for middleware import.
export const locales = ['en', 'ar'] as const;
export const defaultLocale = 'en';
export default {} as Record<string, unknown>;
