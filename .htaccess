# Redirect domain to en/
RewriteEngine On
RewriteBase /
RewriteRule ^$ /en/ [R=302,L]

# Remove trailing slashes from URLs
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)/$ /$1 [L,R=301]

# Handle 404 errors
ErrorDocument 404 /404/index.html
